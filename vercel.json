{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "./dist",
  "framework": "vite",
  "builds": [
    {
      "src": "api/news.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/news",
      "dest": "/api/news.js"
    }
  ]
}
